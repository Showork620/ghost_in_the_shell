{
  "name": "awssaa-dictionary-cards",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:dictionary": "node scripts/build-dictionary.mjs",
    "dev": "npm run sync:dictionary && vite",
    "build": "npm run sync:dictionary && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "^5.4.5",
    "vite": "^8.0.3"
  }
}
