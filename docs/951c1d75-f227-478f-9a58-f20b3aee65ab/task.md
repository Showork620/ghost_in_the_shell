# タスク: モバイル表示最適化 (dvh/dvw)

## フェーズ 1: styles.css の修正 [x]

- [x] `body`, `#root` への `dvh` 及ぶフォールバックの適用
- [x] `.overlay-panel` の高さ・幅指定の修正（`dvh`, `%` ベース）
- [x] `.topbar-wrap` の全幅手法を `vw` 依存から `calc(100% + ...)` 等へ変更
- [x] `@media (max-width: 640px)` 内の viewport 関連指定を全て置換
- [x] `.cards-area` の `padding-bottom` 修正

## フェーズ 2: 検証 [/]
- [ ] モバイルブラウザでの縦スクロール（最下部まで）が途切れないことを確認
- [ ] モバイルブラウザでの横スクロール（揺れ）が発生しないことを確認
- [ ] 各オーバーレイ（Hero, 絞り込み, 比較チートシート）の展開・閉じる操作
